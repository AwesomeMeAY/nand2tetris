Half adder:
	adds two bits
	two input bits (a,b)
	two output bits (sum, carry)
Full adder:
	adds three bits (two and one carry)
	two output bits (sum, carry)
16-bit adder:
	two input 16-bit buses 
16-bit incrementor:
	in + 1
ALU
