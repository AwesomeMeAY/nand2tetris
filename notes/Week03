Clock:
	an oscillator that continuously alternates between 0 and 1 or "tick" "tock"
	Cycle:
		The time between a "tick" and another "tick"
Data Flip Flops:
	One bit input and one bit output
	outputs the input from last cycle
	
Register:
	2 inputs, input, and load, 
	1 output.
	If load is 1 it takes the input and outputs it
	otherwise it outputs the input from last time load was 1
Counters:
	A chip that realises these instructions:
		reset:
			fetch the first instruction 
		fetch:
			Fetch the next instruction
		Goto:
			fetch instruction n
	
		
